rules:
  - id: dart-obfuscation-anonymous-nesting
    pattern: |
      ((...) { ((...) { ((...) { ... })() })() })()
    message: Detect common obfuscation by anonymous nesting.
    severity: WARNING
    languages: 
      - dart
