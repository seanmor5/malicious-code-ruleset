rules:
  - id: lua-obfuscation-reconstruction
    pattern-regex: (\.\.|\.char\(|concat|\.format|gsub|ipairs|\.rep\(|toString)(.*\1){2,}
    message: Detect obfuscation by concatenation of string manipulating methods.
    severity: WARNING
    languages:
      - lua
