rules:
  - id: scala-dynamic-execution-eval
    pattern-either:
      - pattern: $SCALA.interpreter.IMain.eval
      - pattern: $SCALA.mkToolBox().eval
    message: Detect dynamic execution using script manager and runtime.
    severity: ERROR
    languages:
      - scala
