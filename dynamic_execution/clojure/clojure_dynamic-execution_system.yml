rules:
  - id: clojure-dynamic-system-commands
    pattern:
      (require '[clojure.java.shell :refer [$SH]])
      ...
      (def $RESULT ($SH "clojure" ...))
      ...
    message: Detect dynamic execution by system commands.
    severity: ERROR
    languages:
      - clojure
